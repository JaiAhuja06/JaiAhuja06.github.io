<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alex Rivera | Professional Portfolio</title>
    <style>
        :root {
            --primary: #2d3436;
            --accent: #0984e3;
            --bg-light: #f5f7fa;
            --card-bg: #ffffff;
            --text-main: #2d3436;
            --text-muted: #636e72;
            --easing: cubic-bezier(0.165, 0.84, 0.44, 1);
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: 'Inter', 'Segoe UI', sans-serif;
            line-height: 1.6;
            color: var(--text-main);
            background-color: #fff;
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s var(--easing);
        }

        .reveal.active { opacity: 1; transform: translateY(0); }

        header {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(12px);
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.5rem 10%;
            border-bottom: 1px solid rgba(0,0,0,0.05);
        }

        .logo { font-size: 1.2rem; font-weight: 800; letter-spacing: -1px; }
        .logo span { color: var(--accent); }

        nav a {
            text-decoration: none;
            color: var(--text-main);
            font-weight: 600;
            margin-left: 2rem;
            font-size: 0.9rem;
            transition: var(--easing);
        }

        nav a:hover { color: var(--accent); }

        /* --- Updated Hero with Profile Pic --- */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            background: radial-gradient(circle at center, #ffffff 0%, #f0f2f5 100%);
            padding: 0 10%;
        }

        .profile-container {
            width: 150px;
            height: 150px;
            margin-bottom: 2rem;
            border-radius: 50%;
            overflow: hidden;
            border: 4px solid #fff;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            animation: imagePop 0.8s var(--easing) forwards;
        }

        .profile-container img {
            width: 100%;
            height: 100%;
            object-fit: cover; /* Ensures image isn't stretched */
        }

        @keyframes imagePop {
            from { opacity: 0; transform: scale(0.5); }
            to { opacity: 1; transform: scale(1); }
        }

        .hero h1 {
            font-size: clamp(2.5rem, 6vw, 4rem);
            line-height: 1.1;
            margin-bottom: 1rem;
            animation: heroFadeIn 1s var(--easing) 0.2s forwards;
            opacity: 0;
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--text-muted);
            max-width: 600px;
            margin-bottom: 2rem;
            animation: heroFadeIn 1s var(--easing) 0.4s forwards;
            opacity: 0;
        }

        @keyframes heroFadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .btn {
            padding: 0.8rem 2.2rem;
            background: var(--accent);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 700;
            transition: var(--easing);
            box-shadow: 0 10px 20px rgba(9, 132, 227, 0.2);
            opacity: 0;
            animation: heroFadeIn 1s var(--easing) 0.6s forwards;
        }

        .btn:hover { transform: translateY(-3px); box-shadow: 0 15px 30px rgba(9, 132, 227, 0.3); }

        .section { padding: 100px 10%; }
        .section-title { font-size: 2rem; margin-bottom: 3rem; position: relative; display: inline-block; }
        .section-title::after { content: ''; position: absolute; width: 40%; height: 4px; background: var(--accent); bottom: -10px; left: 0; }

        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2.5rem; }
        .card { background: var(--card-bg); padding: 2.5rem; border-radius: 20px; border: 1px solid #edf2f7; transition: var(--easing); }
        .card:hover { border-color: var(--accent); transform: translateY(-10px); box-shadow: 0 20px 40px rgba(0,0,0,0.05); }

        footer { background: var(--primary); color: white; padding: 80px 10% 40px; text-align: center; }
        .footer-links a { color: #fff; margin: 0 15px; text-decoration: none; opacity: 0.6; transition: 0.3s; }
        .footer-links a:hover { opacity: 1; color: var(--accent); }
    </style>
</head>
<body>

    <header>
        <div class="logo">ALEX<span>RIVERA</span></div>
        <nav>
            <a href="#about">About</a>
            <a href="#projects">Projects</a>
            <a href="#contact">Contact</a>
        </nav>
    </header>

    <section class="hero">
        <div class="profile-container">
            <img src="assets/profilepic.jpg" alt="Alex Rivera Professional Portrait">
        </div>
        <h1>Building Digital<br>Systems with Impact.</h1>
        <p>A Senior Solutions Architect dedicated to crafting elegant, scalable code and high-performance user experiences.</p>
        <a href="#projects" class="btn">Explore Work</a>
    </section>

    <section id="about" class="section reveal">
        <h2 class="section-title">The Mission</h2>
        <div style="max-width: 800px;">
            <p style="font-size: 1.1rem; color: var(--text-muted);">
                [DUMMY BIO]: I specialize in bridging the gap between complex technical requirements and seamless user interfaces. 
                I help companies scale their digital presence without compromising on performance or aesthetic quality. 
            </p>
        </div>
    </section>

    <section id="projects" class="section" style="background-color: #fafbfc;">
        <h2 class="section-title reveal">Selected Works</h2>
        <div class="grid">
            <div class="card reveal">
                <h3>Nexus Analytics</h3>
                <p>A real-time data processing engine built to handle high-velocity streams for enterprise fintech applications.</p>
            </div>
            <div class="card reveal">
                <h3>Vanguard UI</h3>
                <p>An open-source design system focused on extreme accessibility and performance-first CSS architecture.</p>
            </div>
            <div class="card reveal">
                <h3>Pulse API</h3>
                <p>Custom RESTful infrastructure designed for global logistics tracking with 99.99% uptime reliability.</p>
            </div>
        </div>
    </section>

    <footer id="contact">
        <div class="footer-content reveal">
            <h2>Let's Talk.</h2>
            <div class="footer-links">
                <a href="mailto:hello@alexrivera.dev">Email</a>
                <a href="#">LinkedIn</a>
                <a href="#">GitHub</a>
            </div>
            <p style="font-size: 0.8rem; opacity: 0.4; margin-top: 40px;">&copy; 2026 Alex Rivera.</p>
        </div>
    </footer>

    <script>
        const observerOptions = { threshold: 0.15 };
        const revealObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('active');
                    revealObserver.unobserve(entry.target);
                }
            });
        }, observerOptions);

        document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));

        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({ behavior: 'smooth' });
            });
        });
    </script>
</body>
</html>
